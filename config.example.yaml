# 配置标记：
# - [必须修改] 首次启动前必须替换，否则服务会拒绝启动或存在安全风险。
# - [按需修改] 仅在部署方式发生变化时调整。
# - [通常不要修改] 已采用推荐默认值。

server:
  # [通常不要修改] Docker 镜像会通过启动参数覆盖为 0.0.0.0:8000。
  listen: "127.0.0.1:8000"
  # [通常不要修改] 单次请求体上限 32 MiB。
  maxBodyBytes: 33554432
  # [通常不要修改] 仅限制客户端上传完整请求体的时间，不限制推理或流式响应。
  readTimeout: 15m
  # [通常不要修改] 管理 API 和非流式请求的最长生命周期。
  requestTimeout: 2h
  # [按需修改] 仅在本地或受信任环境开启，生产环境保持 false。
  swaggerEnabled: false

auth:
  # [通常不要修改] 管理端访问与刷新令牌有效期。
  accessTokenTTL: 15m
  refreshTokenTTL: 720h
  # [按需修改] 外部地址使用 HTTPS 时必须设为 true。
  secureCookies: false

secrets:
  # [必须修改] 至少 32 个字符。快速生成：openssl rand -hex 32
  jwtSecret: "replace-with-at-least-32-characters"
  # [必须修改] Base64 编码的 32 字节密钥。快速生成：openssl rand -base64 32
  # 首次写入账号后必须长期保留，后续更换会导致已有凭据无法解密。
  credentialEncryptionKey: "replace-with-base64-key"

bootstrapAdmin:
  # [按需修改] 仅在数据库中没有管理员时使用。
  username: "admin"
  # [必须修改] 首次启动前替换为强密码；管理员创建成功后建议删除本段。
  password: "replace-with-a-strong-password"

frontend:
  # [通常不要修改] Go 服务同源托管的前端构建目录。
  staticPath: "./frontend/dist"

database:
  # [按需修改] 单实例使用 sqlite；多实例改为 postgres。
  driver: sqlite # sqlite | postgres
  sqlite:
    # [通常不要修改] 相对路径以本配置文件所在目录为基准。
    path: "./data/backend.db"
  postgres:
    # [必须修改：仅 driver=postgres] 填写真实 DSN，并按需启用 sslmode。
    # 也可通过 GROK2API_DATABASE_URL 注入；非空环境变量会覆盖本字段并自动启用 postgres。
    dsn: "postgres://user:password@127.0.0.1:5432/grok2api?sslmode=disable"
    # [按需修改] 根据数据库连接容量调整。
    maxOpenConns: 50
    maxIdleConns: 10

runtimeStore:
  # [按需修改] 单实例使用 memory；多实例改为 redis。
  driver: memory # memory | redis
  redis:
    # [必须修改：仅 driver=redis] 填写 Redis 连接信息。
    address: "127.0.0.1:6379"
    username: ""
    password: ""
    database: 0
    # [通常不要修改] 同一 Redis 中部署多个实例组时才需要更换。
    keyPrefix: "grok2api:"
    # [按需修改] Redis 使用 TLS 时设为 true。
    tls: false

deployment:
  # [通常不要修改] 单实例保持 1；多实例必须同时使用 PostgreSQL、Redis 和共享媒体目录。
  replicas: 1
  # [必须修改：仅 replicas>1] 每个实例使用不同的稳定标识。
  instanceID: ""
  # [必须修改：仅 replicas>1] 同一实例组和共享媒体目录必须使用相同标识。
  clusterID: "grok2api"
  # [必须修改：仅 replicas>1] 确认 media.local.path 已挂载为所有实例共享且可读写的存储。
  sharedMedia: false

media:
  # [通常不要修改] 当前仅支持 local；容量和清理策略在管理端设置。
  driver: local
  local:
    # [按需修改] 图片二进制目录；数据库只保存资源元数据。
    path: "./data/media"

routing:
  # [按需修改] 服务端推理回放缓存：多轮时自动补回客户端未携带的 encrypted_content。
  reasoningReplayEnabled: true
  reasoningReplayTTL: 1h
  reasoningReplayMaxEntries: 10240
  # [按需修改] 按账号隔离上游连接池，适合外部 L4/连接哈希负载均衡；会增加连接数、TLS 握手和文件描述符占用。
  accountIsolatedConnections: false
  # [通常不要修改] 大账号池分段选号默认关闭；启用后仍保留完整池回退和原子并发门禁。
  segmentedSelectorEnabled: false
  segmentedSelectorMinCandidates: 3000
  segmentedSelectorWindowSize: 64

audit:
  # [通常不要修改] 审计批量写入参数；bufferSize 的修改需重启生效。
  bufferSize: 16384
  batchSize: 256
  flushInterval: 250ms
  # [通常不要修改] 需要调用方确认提交的审计最多聚合等待该时长；建议保持 1ms–50ms。
  commitDelay: 5ms
  # [通常不要修改] enforce 会在异常持续超过宽限期后暂停新的推理请求；确认发生数据丢失时会立即暂停。
  ledgerMode: enforce # observe | enforce
  ledgerFailureThreshold: 1
  ledgerUnhealthyGrace: 10s
  ledgerQueueHighWatermarkPercent: 90

# [按需修改] Mihomo 出口代理接入（Provider Web）参数。运行期可在管理端设置页热更新；
# 不配置本段时使用下列默认值；启用开关（mihomoEnabled）在管理端设置页。
provider:
  web:
    # [通常不要修改] 出口 IP 探测经此本地 mihomo 代理端口转发。
    mihomoExitProbeProxyURL: "http://127.0.0.1:7890"
    # [通常不要修改] IP 回显端点；也支持纯文本 IP 响应体。
    mihomoIPProbeURL: "https://1.1.1.1/cdn-cgi/trace"
    # [通常不要修改] 出口 IP 未变化时的重试次数上限（加首次尝试共 maxAttempts+1 次切换）。
    mihomoMaxAttempts: 3
    # [通常不要修改] 每次尝试内出口 IP 轮询的截止时长。
    mihomoVerifyTimeout: 15s
    # [按需启用] 双通道测试组（质量守卫探测专用，见 tools/mihomo/mihomo-dual-channel.yaml）。
    # mihomoTestProxyURL 留空 = 禁用测试通道（legacy 单组行为）；配置后测试组
    # 经独立 listener（默认 7891）直连，守卫自由切换探测成员，生产出口零扰动。
    mihomoTestGroupName: "XAI-TEST-GROUP"
    mihomoTestProxyURL: "http://127.0.0.1:7891"
    # [按需启用] 主动延迟探测端点（走 mihomo 代理）：配置后 select 组切换优先
    # 选择历史延迟最低的节点；留空 = 禁用主动探测（保持回退首可用节点现状）。
    mihomoDelayProbeURL: ""

# [按需启用] Grok Build 出口质量守护。守护程序内置于主容器（s6-overlay 服务
# egress-quality-guard），随主容器自动启动，仅 enabled 时产生探测流量。
qualityGuard:
  enabled: false
  # 主程序会自动创建并复用不可导出的内部探测身份，无需配置 Client Key。
  model: "grok-4.5"
  mode: hybrid # passive | active | hybrid
  activeInterval: 30m
  passivePollInterval: 5s
  softTPS: 500
  hardTPS: 1000
  consecutiveSoft: 2
  consecutiveErrors: 2
  quarantineDuration: 5m
  noAccountBackoff: 5m
  minimumHealthyNodes: 3
  maxOutputTokens: 384
  failClosed: false
  minimumGenerationWindow: 1s
  # 可选的受信任换 IP Webhook；启用时同时填写 rotatableNodeIDs。
  # 指向 Go 内置轮换端点时（推荐，组模型下自动把共享 Mihomo 组出口切到下一
  # 健康节点，nodeId 仅作契约校验）：
  #   rotationURL: "http://127.0.0.1:8000/api/internal/v1/quality-guard/egress-mihomo/rotate"
  # 该端点位于 quality-guard 内部认证组内（与 guard 其他内部调用同一令牌）。
  rotationURL: ""
  # rotationToken：内置端点要求与主服务派生的 quality-guard 内部令牌一致
  # （即 bootstrap.json 中的 internal_token：主服务写入共享状态目录后，把该
  # 值拷贝到此处即可；它由 jwtSecret 确定性派生，永不通过 API 暴露）。
  # 仅当 rotationURL 指向外部轮换服务（如 session_rotator.py）时，才改为
  # 填写该外部服务自己的令牌。
  rotationToken: ""
  rotationTimeout: 45s
  nodeIDs: []
  rotatableNodeIDs: []
